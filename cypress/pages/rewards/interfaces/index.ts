export interface CheckTitleProps {
  title: string;
  color: "black";
  visible: Boolean;
}

export interface CheckSelectProps {
  value: "All" | "January" | "February" | "March";
  visible: Boolean;
}
